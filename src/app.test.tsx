import{render,screen}from'@testing-library/react';import{describe,it,expect,beforeEach}from'vitest';import{App}from'./main';
describe('app shell',()=>{beforeEach(()=>{location.hash='/' ; localStorage.clear()});it('renders home dashboard route',()=>{render(<App/>);expect(screen.getByText(/Home dashboard/)).toBeInTheDocument()});it('renders unit selection',()=>{location.hash='/units';render(<App/>);expect(screen.getByText(/Unit selection/)).toBeInTheDocument();expect(screen.getAllByText('Start').length).toBeGreaterThan(0)});it('renders parent dashboard',()=>{location.hash='/parent';render(<App/>);expect(screen.getByText(/Parent dashboard/)).toBeInTheDocument()})});
