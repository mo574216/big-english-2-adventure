import{describe,it,expect,beforeEach}from'vitest';import{completeActivity,dictationFeedback,loadProgress,normalizeAnswer,scoreAnswer}from'./learning';
describe('learning services',()=>{beforeEach(()=>localStorage.clear());it('normalises answers',()=>expect(normalizeAnswer('  He’s, READING! ')).toBe("he's reading"));it('scores alternatives',()=>expect(scoreAnswer('Five',['5','five'])).toBe(true));it('compares dictation by word',()=>expect(dictationFeedback('there is board','there is one board')[2].correct).toBe(false));it('persists progress',()=>{completeActivity('a',true);expect(loadProgress().stars).toBe(2);expect(loadProgress().completed).toContain('a')})});
